# GB28181 智能��频管理平台 — 部署架构

## 服务端口规划

| 服务 | 端口 | 协议 | 说明 | 开放范围 |
|------|------|------|------|----------|
| Nginx | 80/443 | TCP | 前端 SPA + 反向代理 API | 用户浏览器 |
| .NET Core App | 5000 | TCP | Web API + SignalR | Nginx 反向代理 |
| .NET Core App | 5060 | UDP+TCP | SIP 信令 | 摄像机网段 |
| ZLMediaKit | 8080 | TCP | REST API | 内部(.NET调用) |
| ZLMediaKit | 30000-30249 | UDP+TCP | RTP 收流 | 摄像机��段 |
| ZLMediaKit | 8443 | TCP(UDP) | WebRTC WHEP | 用户浏览器 |
| ZLMediaKit | 554 | TCP | RTSP (可选) | 内部 |
| ZLMediaKit | 8000 | TCP | HTTP-FLV (可选) | 内部 |
| MySQL | 3306 | TCP | 数据库 | 内部 |
| Redis | 6379 | TCP | 缓存+消息 | 内部 |
| Qwen 3.5 | 公司内网 | TCP | 大模型 API | 内部 |

## 部署拓扑

```
┌─────────────────────────────────────────────────────────────┐
│                    服务器 (Linux / Windows)                   │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  Nginx (:80/:443)                                    │   │
│  │  ├── / → Vue 3 SPA 静态文件                           │   │
│  │  ├── /api → 反向代理 → .NET :5000                     │   │
│  │  └── /hubs → 反向代理 → .NET :5000 (WebSocket)        │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  .NET Core App                                        │   │
│  │  ├── :5000 HTTP API + SignalR                         │   │
│  │  ├── :5060 SIP Server (UDP+TCP)                       │   │
│  │  └── 包含: SIP服务 + 诊断引擎 + AI Agent              │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  ZLMediaKit                                           │   │
│  │  ├── :8080 REST API                                   │   │
│  │  ├── :30000-30249 RTP 收流                             │   │
│  │  ├── :8443 WebRTC                                     │   │
│  │  └── :554 RTSP (可选)                                  │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐                         │
│  │ MySQL :3306  │  │ Redis :6379  │                         │
│  └──────────────┘  └──────────────┘                         │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  Qwen 3.5 LLM (公司自建，独立服务器)                   │   │
│  │  OpenAI 兼容 API                                      │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

## Nginx 配置要点

```nginx
server {
    listen 80;
    server_name your-domain;

    # 前端 SPA
    location / {
        root /var/www/vms-web;
        try_files $uri $uri/ /index.html;
    }

    # API 反向代理
    location /api/ {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # SignalR WebSocket
    location /hubs/ {
        proxy_pass http://127.0.0.1:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
```

## 防火墙规则

必须开放的端口：
- **80/443** — 用户浏览器访问
- **5060 UDP+TCP** — 摄像机 SIP 信令
- **30000-30249 UDP+TCP** — 摄像机 RTP 推流
- **8443 TCP+UDP** — 用户浏览器 WebRTC

内部端口（不对外开放）：
- 5000, 8080, 3306, 6379
