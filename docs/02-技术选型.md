# GB28181 智能视频管理平台 — 技术选型

## 选型结果

| 组件 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 后端 | C# .NET Core (ASP.NET Core) | .NET 9 | 统一后端语言 |
| 前端 | Vue 3 + Element Plus + TypeScript | Vue 3.x | 国内主流方案 |
| SIP 服务器 | sipsorcery | 10.0.3 (NuGet) | 纯 C# SIP 库，支持 UAC/UAS |
| 流媒体 | ZLMediaKit | 最新 | GB28181 RTP→WebRTC，业界最成熟 |
| 数据库 | MySQL | 现有环境 | SqlSugar ORM，CodeFirst 自动建表 |
| ORM | SqlSugarCore | 5.1.4.x | 国产 ORM，支持 CodeFirst |
| 缓存 | Redis | — | 设备在线状态、事件发布 |
| 大模型 | 公司自建 Qwen 3.5 | OpenAI 兼容 API | Function Calling 模式 |
| 浏览器自动化 | Playwright | Headless Chromium | 访问摄像机网页检查配置 |
| 实时推送 | SignalR | — | 设备状态变更推送 |
| 日志 | Serilog | — | 结构化日志 |

## 选型决策过程

### 方案对比

评估了 3 种方案：

| 维度 | 方案 A: 纯 .NET 全新构建 | 方案 B: Go(JydCMS) + .NET 混合 | 方案 C: .NET + ZLMediaKit |
|------|---|---|---|
| SIP 服务器 | .NET sipsorcery 自研 | 复用 Go JydCMS | .NET sipsorcery 自研 |
| 流媒体 | .NET 自研（RTP→WebRTC） | 复用 Go JydSMS | ZLMediaKit（C++，业界主流） |
| 技术栈统一 | 高（全 .NET） | 低（Go + C# 双语言） | 中（C# + C++ 组件） |
| 开发工作量 | 极大（流媒体自研风险高） | 最小（核心复用） | 中等 |
| 流媒体成熟度 | 低 | 高 | 极高 |
| 推荐度 | 不推荐 | 备选（快速交付） | **最终选择** |

### 选择方案 C 的原因

1. **.NET 统一后端语言** — SIP 服务器和 Web API 深度集成，诊断引擎可直接在 SIP 层触发
2. **ZLMediaKit 专业流媒体** — 原生支持 GB28181 PS 解封装 → WebRTC，单机可支撑数千路
3. **sipsorcery 支持 UAS** — 完全可以用来构建 SIP 服务器
4. **可复用现有代码** — VmsClient.Protocol 的 XML Builder/Parser 模式可直接复用

### SqlSugar 选择原因

- 用户要求使用 SqlSugar 而非 EF Core
- CodeFirst 自动建表，程序启动时自动生成/更新表结构
- 国产 ORM，对 MySQL 支持好，API 简洁
