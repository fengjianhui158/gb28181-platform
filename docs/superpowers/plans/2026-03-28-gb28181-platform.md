# GB28181 智能视频管理平台 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 Phase 1 基础框架之上，完成设备管理增强、科技风 UI、视频预览、离线诊断引擎、AI 问答 Agent 的完整实现。

**Architecture:** ASP.NET Core 8 后端 + Vue 3 科技风前端 + ZLMediaKit 流媒体 + Playwright 浏览器诊断 + Qwen 3.5 AI Agent。后端分 7 层 Clean Architecture，前端 4 页面 SPA。

**Tech Stack:** .NET 8 LTS, Vue 3 + Element Plus + TypeScript, SqlSugar + MySQL, Redis 5.0.14, sipsorcery 10.0.3, ZLMediaKit, Playwright, Qwen 3.5 (OpenAI API), SignalR, Serilog

---

## 现有代码状态

Phase 1 已完成：
- SIP 服务器（REGISTER/心跳/认证）✅
- 设备管理 API (CRUD) ✅
- 7 个数据库实体 + CodeFirst ✅
- 前端 4 页面基础框架 ✅
- SignalR 实时推送 ✅
- DeviceMonitorService 心跳监控 ✅

待实现：
- .NET 9 → .NET 8 降级
- 科技风 UI 主题
- SIP 目录保存、报警处理
- ZLMediaKit 集成 + WebRTC 预览
- 诊断引擎（Ping/Port/Browser）
- AI Agent（Qwen Function Calling）

---

## File Structure

### 后端新增/修改文件

```
src/
├── GB28181Platform.Domain/
│   └── Entities/                        # 已有，无需修改
│
├── GB28181Platform.Application/         # 当前为空，需实现
│   ├── Devices/
│   │   ├── IDeviceAppService.cs         # CREATE: 设备应用服务接口
│   │   └── DeviceAppService.cs          # CREATE: 设备应用服务实现
│   └── Streams/
│       ├── IStreamAppService.cs         # CREATE: 流媒体应用服务接口
│       └── StreamAppService.cs          # CREATE: 流媒体应用服务实现
│
├── GB28181Platform.Sip/
│   ├── Server/Gb28181SipServer.cs       # MODIFY: 完善目录保存和报警处理
│   └── Handlers/
│       ├── CatalogHandler.cs            # CREATE: 目录查询结果保存到 DB
│       └── InviteHandler.cs             # CREATE: INVITE 发起（拉流）
│
├── GB28181Platform.Diagnostic/          # 当前为空，需全部实现
│   ├── Engine/
│   │   ├── IDiagnosticEngine.cs         # CREATE: 诊断引擎接口
│   │   └── DiagnosticEngine.cs          # CREATE: 诊断流水线
│   ├── Steps/
│   │   ├── IDiagnosticStep.cs           # CREATE: 诊断步骤接口
│   │   ├── PingCheckStep.cs             # CREATE: ICMP Ping 检测
│   │   ├── PortCheckStep.cs             # CREATE: TCP 端口检测
│   │   └── BrowserCheckStep.cs          # CREATE: 浏览器检查步骤
│   └── Browser/
│       └── CameraBrowserAgent.cs        # CREATE: Playwright + Qwen 视觉
│
├── GB28181Platform.AiAgent/             # 当前为空，需全部实现
│   ├── IQwenClient.cs                   # CREATE: Qwen 客户端接口
│   ├── QwenClient.cs                    # CREATE: OpenAI 兼容 HTTP 客户端
│   ├── IAiAgentService.cs               # CREATE: AI Agent 服务接口
│   ├── AiAgentService.cs                # CREATE: AI Agent 服务（Function Calling 循环）
│   ├── Functions/
│   │   ├── IAgentFunction.cs            # CREATE: Agent 函数接口
│   │   ├── GetDeviceStatusFunction.cs   # CREATE
│   │   ├── GetDiagnosticLogsFunction.cs # CREATE
│   │   ├── GetDeviceInfoFunction.cs     # CREATE
│   │   ├── ListOfflineDevicesFunction.cs# CREATE
│   │   └── FunctionRegistry.cs          # CREATE: 函数注册表
│   └── Prompts/
│       └── SystemPrompts.cs             # CREATE: System Prompt 模板
│
├── GB28181Platform.Infrastructure/
│   ├── Database/DbContext.cs            # 已有，无需修改
│   ├── Redis/RedisService.cs            # CREATE: Redis 封装
│   └── MediaServer/
│       ├── IZlmClient.cs               # CREATE: ZLMediaKit 客户端接口
│       └── ZlmClient.cs                # CREATE: ZLMediaKit REST API 调用
│
└── GB28181Platform.Api/
    ├── Controllers/
    │   ├── DeviceController.cs          # MODIFY: 使用 AppService
    │   ├── StreamController.cs          # CREATE: 流媒体控制器
    │   ├── DiagnosticController.cs      # MODIFY: 接入诊断引擎
    │   └── AiAgentController.cs         # MODIFY: 接入 AI Agent
    ├── BackgroundServices/
    │   ├── DeviceMonitorService.cs      # MODIFY: 触发自动诊断
    │   └── DiagnosticWorkerService.cs   # CREATE: 诊断任务后台执行
    └── Program.cs                       # MODIFY: 注册新服务
```

### 前端新增/修改文件

```
frontend/vms-web/src/
├── styles/
│   ├── theme.css                        # CREATE: 科技风全局主题
│   └── animations.css                   # CREATE: 动画效果
├── views/
│   ├── DeviceList.vue                   # MODIFY: 科技风改造
│   ├── LivePreview.vue                  # REWRITE: WebRTC 播放器
│   ├── DiagnosticPanel.vue              # MODIFY: 时间线样式
│   └── AiChat.vue                       # MODIFY: 终端风格 + 打字机效果
├── api/
│   └── stream.ts                        # CREATE: 流媒体 API
├── App.vue                              # MODIFY: 科技风导航
└── main.ts                              # MODIFY: 引入主题
```

---

## Task 1: .NET 9 → .NET 8 降级

**Files:**
- Modify: `src/GB28181Platform.Domain/GB28181Platform.Domain.csproj`
- Modify: `src/GB28181Platform.Application/GB28181Platform.Application.csproj`
- Modify: `src/GB28181Platform.Sip/GB28181Platform.Sip.csproj`
- Modify: `src/GB28181Platform.Diagnostic/GB28181Platform.Diagnostic.csproj`
- Modify: `src/GB28181Platform.AiAgent/GB28181Platform.AiAgent.csproj`
- Modify: `src/GB28181Platform.Infrastructure/GB28181Platform.Infrastructure.csproj`
- Modify: `src/GB28181Platform.Api/GB28181Platform.Api.csproj`

- [ ] **Step 1: 修改所有 .csproj 目标框架**

将每个 `.csproj` 中的 `<TargetFramework>net9.0</TargetFramework>` 改为 `<TargetFramework>net8.0</TargetFramework>`。

对于 Web 项目 (Api)，将 `net9.0` 改为 `net8.0`。

- [ ] **Step 2: 降级不兼容的 NuGet 包**

检查 Serilog.AspNetCore 10.0.0 是否兼容 .NET 8，如不兼容降级到 8.x。
SIPSorcery 10.0.3 和 SqlSugarCore 5.1.4.214 均兼容 .NET 8，无需变更。

- [ ] **Step 3: 编译验证**

Run: `dotnet build GB28181Platform.sln`
Expected: Build succeeded. 0 Errors.

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "build: downgrade from .NET 9 to .NET 8 LTS"
```

---

## Task 2: 科技风 UI 主题

**Files:**
- Create: `frontend/vms-web/src/styles/theme.css`
- Create: `frontend/vms-web/src/styles/animations.css`
- Modify: `frontend/vms-web/src/main.ts`
- Modify: `frontend/vms-web/src/App.vue`
- Modify: `frontend/vms-web/index.html`

- [ ] **Step 1: 创建科技风全局主题文件**

Create `frontend/vms-web/src/styles/theme.css`:

```css
/* === GB28181 科技风主题 === */
:root {
  /* 主色调 */
  --tech-primary: #00d4ff;
  --tech-primary-light: #00f0ff;
  --tech-accent: #7b61ff;

  /* 状态色 */
  --tech-online: #00ff88;
  --tech-offline: #ff4444;
  --tech-warning: #ffaa00;

  /* 背景层次 */
  --tech-bg-deep: #060a14;
  --tech-bg-base: #0a0e1a;
  --tech-bg-card: #111827;
  --tech-bg-elevated: #1a1f36;
  --tech-bg-hover: #1e2642;

  /* 文字 */
  --tech-text-primary: #e0e6ed;
  --tech-text-secondary: #8892a4;
  --tech-text-muted: #4a5568;

  /* 边框 */
  --tech-border: rgba(0, 212, 255, 0.15);
  --tech-border-glow: rgba(0, 212, 255, 0.4);

  /* Element Plus 覆盖 */
  --el-color-primary: #00d4ff;
  --el-color-primary-light-3: #33ddff;
  --el-color-primary-light-5: #66e6ff;
  --el-color-primary-light-7: #99eeff;
  --el-color-primary-light-9: #ccf7ff;
  --el-color-primary-dark-2: #00a8cc;
  --el-bg-color: #0a0e1a;
  --el-bg-color-overlay: #111827;
  --el-bg-color-page: #060a14;
  --el-text-color-primary: #e0e6ed;
  --el-text-color-regular: #8892a4;
  --el-border-color: rgba(0, 212, 255, 0.15);
  --el-border-color-light: rgba(0, 212, 255, 0.1);
  --el-fill-color-blank: #111827;
  --el-mask-color: rgba(6, 10, 20, 0.8);
}

/* 全局样式 */
body {
  background: var(--tech-bg-deep);
  color: var(--tech-text-primary);
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
}

/* 等宽数字字体 */
.tech-mono {
  font-family: 'JetBrains Mono', 'Source Code Pro', 'Consolas', monospace;
}

/* 科技风卡片 */
.tech-card {
  background: var(--tech-bg-card);
  border: 1px solid var(--tech-border);
  border-radius: 8px;
  transition: border-color 0.3s, box-shadow 0.3s;
}

.tech-card:hover {
  border-color: var(--tech-border-glow);
  box-shadow: 0 0 20px rgba(0, 212, 255, 0.1);
}

/* 状态指示灯 */
.status-dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  display: inline-block;
}

.status-dot--online {
  background: var(--tech-online);
  box-shadow: 0 0 8px var(--tech-online);
  animation: pulse 2s infinite;
}

.status-dot--offline {
  background: var(--tech-offline);
  box-shadow: 0 0 8px var(--tech-offline);
}

/* 大号数字指标 */
.tech-metric {
  font-family: 'JetBrains Mono', monospace;
  font-size: 2.5rem;
  font-weight: 700;
  color: var(--tech-primary);
  text-shadow: 0 0 20px rgba(0, 212, 255, 0.3);
}

/* 微光表格行 */
.el-table {
  --el-table-bg-color: var(--tech-bg-card);
  --el-table-tr-bg-color: var(--tech-bg-card);
  --el-table-header-bg-color: var(--tech-bg-elevated);
  --el-table-row-hover-bg-color: var(--tech-bg-hover);
  --el-table-border-color: var(--tech-border);
  --el-table-text-color: var(--tech-text-primary);
  --el-table-header-text-color: var(--tech-text-secondary);
}

/* Element Plus 按钮覆盖 */
.el-button--primary {
  --el-button-bg-color: transparent;
  --el-button-border-color: var(--tech-primary);
  --el-button-text-color: var(--tech-primary);
  --el-button-hover-bg-color: rgba(0, 212, 255, 0.1);
  --el-button-hover-border-color: var(--tech-primary-light);
  --el-button-hover-text-color: var(--tech-primary-light);
}

/* 输入框 */
.el-input__wrapper {
  background-color: var(--tech-bg-card) !important;
  box-shadow: 0 0 0 1px var(--tech-border) inset !important;
}

.el-input__wrapper:hover {
  box-shadow: 0 0 0 1px var(--tech-border-glow) inset !important;
}

/* 导航菜单 */
.el-menu {
  --el-menu-bg-color: var(--tech-bg-base);
  --el-menu-text-color: var(--tech-text-secondary);
  --el-menu-active-color: var(--tech-primary);
  --el-menu-hover-bg-color: var(--tech-bg-hover);
  border-right-color: var(--tech-border) !important;
}

/* 标签 */
.el-tag {
  border-color: transparent;
}
```

- [ ] **Step 2: 创建动画文件**

Create `frontend/vms-web/src/styles/animations.css`:

```css
/* 呼吸灯 */
@keyframes pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.4; }
}

/* 微光扫过 */
@keyframes glow-sweep {
  0% { background-position: -200% center; }
  100% { background-position: 200% center; }
}

/* 淡入上移 */
@keyframes fade-in-up {
  from { opacity: 0; transform: translateY(10px); }
  to { opacity: 1; transform: translateY(0); }
}

.animate-fade-in {
  animation: fade-in-up 0.3s ease-out;
}

/* 打字机光标 */
@keyframes blink-cursor {
  0%, 100% { border-right-color: var(--tech-primary); }
  50% { border-right-color: transparent; }
}

.typing-cursor {
  border-right: 2px solid var(--tech-primary);
  animation: blink-cursor 1s infinite;
  padding-right: 2px;
}
```

- [ ] **Step 3: 在 main.ts 引入主题**

在 `frontend/vms-web/src/main.ts` 中，在 Element Plus 的 CSS 之后添加：

```typescript
import './styles/theme.css'
import './styles/animations.css'
```

- [ ] **Step 4: 改造 App.vue 为科技风导航**

重写 `App.vue` 的 `<template>` 和 `<style>`：
- 左侧深色导航栏，宽度 200px
- 顶部状态概览条（显示在线设备数/总数）
- 导航图标和文字使用 tech-primary 色
- 右侧内容区域使用 tech-bg-deep 背景

- [ ] **Step 5: 在 index.html 引入 JetBrains Mono 字体**

```html
<link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;700&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
```

- [ ] **Step 6: 验证科技风效果**

Run: `cd frontend/vms-web && npm run dev`
在浏览器中确认：深色背景、荧光蓝色调、表格和导航风格正确。

- [ ] **Step 7: Commit**

```bash
git add frontend/vms-web/src/styles/ frontend/vms-web/src/main.ts frontend/vms-web/src/App.vue frontend/vms-web/index.html
git commit -m "feat(frontend): add sci-fi dark theme with glow effects"
```

---

## Task 3: 科技风改造设备列表页

**Files:**
- Modify: `frontend/vms-web/src/views/DeviceList.vue`

- [ ] **Step 1: 重写 DeviceList.vue**

改造要点：
- 顶部统计卡片区（总数/在线/离线，使用 tech-metric 大号数字 + 呼吸灯状态点）
- 搜索筛选栏使用暗色输入框
- 设备表格使用科技风表格样式
- 状态列：在线=绿色呼吸灯+文字，离线=红色静止灯+文字
- 操作列：预览/诊断按钮使用边框型按钮
- 集成 SignalR 实时接收设备状态变更

```vue
<template>
  <div class="device-page">
    <!-- 统计卡片 -->
    <div class="stats-row">
      <div class="tech-card stat-card">
        <div class="stat-label">设备总数</div>
        <div class="tech-metric">{{ devices.length }}</div>
      </div>
      <div class="tech-card stat-card">
        <div class="stat-label">在线</div>
        <div class="tech-metric" style="color: var(--tech-online)">{{ onlineCount }}</div>
      </div>
      <div class="tech-card stat-card">
        <div class="stat-label">离线</div>
        <div class="tech-metric" style="color: var(--tech-offline)">{{ offlineCount }}</div>
      </div>
    </div>

    <!-- 筛选栏 -->
    <div class="tech-card filter-bar">
      <el-select v-model="statusFilter" placeholder="状态筛选" clearable @change="fetchDevices">
        <el-option label="在线" value="Online" />
        <el-option label="离线" value="Offline" />
      </el-select>
      <el-input v-model="searchText" placeholder="搜索设备ID/名称/IP" clearable class="search-input" />
    </div>

    <!-- 设备表格 -->
    <div class="tech-card">
      <el-table :data="filteredDevices" v-loading="loading" stripe>
        <el-table-column prop="id" label="设备编码" width="220">
          <template #default="{ row }">
            <span class="tech-mono">{{ row.id }}</span>
          </template>
        </el-table-column>
        <el-table-column prop="name" label="名称" />
        <el-table-column prop="remoteIp" label="IP 地址">
          <template #default="{ row }">
            <span class="tech-mono">{{ row.remoteIp }}</span>
          </template>
        </el-table-column>
        <el-table-column prop="manufacturer" label="厂商" width="100" />
        <el-table-column prop="status" label="状态" width="100">
          <template #default="{ row }">
            <span class="status-indicator">
              <span :class="['status-dot', row.status === 1 ? 'status-dot--online' : 'status-dot--offline']" />
              {{ row.status === 1 ? '在线' : '离线' }}
            </span>
          </template>
        </el-table-column>
        <el-table-column label="操作" width="200">
          <template #default="{ row }">
            <el-button size="small" @click="goPreview(row)">预览</el-button>
            <el-button size="small" @click="goDiagnostic(row)">诊断</el-button>
          </template>
        </el-table-column>
      </el-table>
    </div>
  </div>
</template>
```

- [ ] **Step 2: 验证页面效果**

Run: `cd frontend/vms-web && npm run dev`
确认统计卡片、表格、状态灯效果正确。

- [ ] **Step 3: Commit**

```bash
git add frontend/vms-web/src/views/DeviceList.vue
git commit -m "feat(frontend): redesign device list with sci-fi theme"
```

---

## Task 4: ZLMediaKit 客户端

**Files:**
- Create: `src/GB28181Platform.Infrastructure/MediaServer/IZlmClient.cs`
- Create: `src/GB28181Platform.Infrastructure/MediaServer/ZlmClient.cs`
- Modify: `src/GB28181Platform.Infrastructure/ServiceCollectionExtensions.cs`

- [ ] **Step 1: 创建 IZlmClient 接口**

```csharp
namespace GB28181Platform.Infrastructure.MediaServer;

public interface IZlmClient
{
    /// <summary>打开 RTP 收流端口</summary>
    Task<OpenRtpResult?> OpenRtpServerAsync(string streamId, int? port = null);

    /// <summary>关闭 RTP 收流端口</summary>
    Task<bool> CloseRtpServerAsync(string streamId);

    /// <summary>获取流列表</summary>
    Task<List<MediaStream>> GetMediaListAsync();

    /// <summary>关闭流</summary>
    Task<bool> CloseStreamAsync(string schema, string vhost, string app, string stream);

    /// <summary>获取 WebRTC 播放地址</summary>
    string GetWebRtcPlayUrl(string app, string streamId);
}

public class OpenRtpResult
{
    public int Code { get; set; }
    public int Port { get; set; }
}

public class MediaStream
{
    public string App { get; set; } = "";
    public string Stream { get; set; } = "";
    public string Schema { get; set; } = "";
    public int TotalReaderCount { get; set; }
}
```

- [ ] **Step 2: 实现 ZlmClient**

```csharp
namespace GB28181Platform.Infrastructure.MediaServer;

public class ZlmClient : IZlmClient
{
    private readonly HttpClient _http;
    private readonly string _baseUrl;
    private readonly string _secret;

    public ZlmClient(IConfiguration config)
    {
        _baseUrl = config["ZLMediaKit:BaseUrl"] ?? "http://127.0.0.1:8080";
        _secret = config["ZLMediaKit:Secret"] ?? "";
        _http = new HttpClient { BaseAddress = new Uri(_baseUrl) };
    }

    public async Task<OpenRtpResult?> OpenRtpServerAsync(string streamId, int? port = null)
    {
        var url = $"/index/api/openRtpServer?secret={_secret}&stream_id={streamId}&port={port ?? 0}&enable_tcp=1";
        var resp = await _http.GetFromJsonAsync<OpenRtpResult>(url);
        return resp;
    }

    public async Task<bool> CloseRtpServerAsync(string streamId)
    {
        var url = $"/index/api/closeRtpServer?secret={_secret}&stream_id={streamId}";
        var resp = await _http.GetFromJsonAsync<ZlmBaseResult>(url);
        return resp?.Code == 0;
    }

    public async Task<List<MediaStream>> GetMediaListAsync()
    {
        var url = $"/index/api/getMediaList?secret={_secret}";
        var resp = await _http.GetFromJsonAsync<ZlmMediaListResult>(url);
        return resp?.Data ?? new();
    }

    public async Task<bool> CloseStreamAsync(string schema, string vhost, string app, string stream)
    {
        var url = $"/index/api/close_streams?secret={_secret}&schema={schema}&vhost={vhost}&app={app}&stream={stream}";
        var resp = await _http.GetFromJsonAsync<ZlmCloseResult>(url);
        return (resp?.CountClosed ?? 0) > 0;
    }

    public string GetWebRtcPlayUrl(string app, string streamId)
    {
        return $"{_baseUrl}/index/api/webrtc?app={app}&stream={streamId}&type=play";
    }
}

internal class ZlmBaseResult { public int Code { get; set; } }
internal class ZlmMediaListResult { public int Code { get; set; } public List<MediaStream> Data { get; set; } = new(); }
internal class ZlmCloseResult { public int Code { get; set; } public int CountClosed { get; set; } }
```

- [ ] **Step 3: 注册到 DI**

在 `ServiceCollectionExtensions.cs` 中添加：

```csharp
services.AddSingleton<IZlmClient, ZlmClient>();
```

- [ ] **Step 4: 编译验证**

Run: `dotnet build GB28181Platform.sln`
Expected: Build succeeded.

- [ ] **Step 5: Commit**

```bash
git add src/GB28181Platform.Infrastructure/
git commit -m "feat(infra): add ZLMediaKit REST API client"
```

---

## Task 5: SIP INVITE 拉流 + 流媒体控制器

**Files:**
- Create: `src/GB28181Platform.Sip/Handlers/InviteHandler.cs`
- Create: `src/GB28181Platform.Application/Streams/IStreamAppService.cs`
- Create: `src/GB28181Platform.Application/Streams/StreamAppService.cs`
- Create: `src/GB28181Platform.Api/Controllers/StreamController.cs`
- Modify: `src/GB28181Platform.Api/Program.cs`

- [ ] **Step 1: 创建 InviteHandler**

实现 SIP INVITE 发起，SDP 中媒体地址指向 ZLMediaKit 的 RTP 端口。

```csharp
namespace GB28181Platform.Sip.Handlers;

public class InviteHandler
{
    private readonly Gb28181SipServer _sipServer;
    private readonly ILogger<InviteHandler> _logger;

    public InviteHandler(Gb28181SipServer sipServer, ILogger<InviteHandler> logger)
    {
        _sipServer = sipServer;
        _logger = logger;
    }

    /// <summary>
    /// 向摄像机发送 INVITE 请求拉流
    /// </summary>
    public async Task<bool> SendInviteAsync(string deviceId, string channelId, string mediaIp, int mediaPort)
    {
        var session = _sipServer.SessionManager.GetSession(deviceId);
        if (session == null)
        {
            _logger.LogWarning("设备 {DeviceId} 不在线，无法拉流", deviceId);
            return false;
        }

        // 构建 SDP (PS 流, RTP over UDP)
        var sdp = $@"v=0
o={_sipServer.Options.ServerId} 0 0 IN IP4 {mediaIp}
s=Play
c=IN IP4 {mediaIp}
t=0 0
m=video {mediaPort} RTP/AVP 96
a=recvonly
a=rtpmap:96 PS/90000
y=0100000001";

        // 通过 SIP 发送 INVITE
        var targetUri = session.RemoteEndPoint;
        _logger.LogInformation("发送 INVITE 到 {DeviceId}/{ChannelId}, 媒体地址 {Ip}:{Port}",
            deviceId, channelId, mediaIp, mediaPort);

        // TODO: 使用 sipsorcery SIPTransport 发送 INVITE 事务
        // 这里先记录流程，后续完善 SIP 事务处理
        return true;
    }
}
```

- [ ] **Step 2: 创建 StreamAppService**

```csharp
namespace GB28181Platform.Application.Streams;

public interface IStreamAppService
{
    Task<PlayResult> PlayAsync(string deviceId, string channelId);
    Task<bool> StopAsync(string deviceId, string channelId);
}

public class PlayResult
{
    public bool Success { get; set; }
    public string? WebRtcUrl { get; set; }
    public string? Message { get; set; }
}
```

```csharp
namespace GB28181Platform.Application.Streams;

public class StreamAppService : IStreamAppService
{
    private readonly IZlmClient _zlm;
    private readonly InviteHandler _invite;
    private readonly IConfiguration _config;
    private readonly ILogger<StreamAppService> _logger;

    public StreamAppService(IZlmClient zlm, InviteHandler invite,
        IConfiguration config, ILogger<StreamAppService> logger)
    {
        _zlm = zlm;
        _invite = invite;
        _config = config;
        _logger = logger;
    }

    public async Task<PlayResult> PlayAsync(string deviceId, string channelId)
    {
        var streamId = $"{deviceId}_{channelId}";

        // 1. 向 ZLMediaKit 申请 RTP 端口
        var rtp = await _zlm.OpenRtpServerAsync(streamId);
        if (rtp == null || rtp.Code != 0)
            return new PlayResult { Success = false, Message = "ZLMediaKit openRtpServer 失败" };

        // 2. 获取 ZLMediaKit 的 IP
        var mediaIp = _config["ZLMediaKit:MediaIp"] ?? "127.0.0.1";

        // 3. 向摄像机发送 INVITE
        var ok = await _invite.SendInviteAsync(deviceId, channelId, mediaIp, rtp.Port);
        if (!ok)
        {
            await _zlm.CloseRtpServerAsync(streamId);
            return new PlayResult { Success = false, Message = "INVITE 发送失败" };
        }

        // 4. 返回 WebRTC 播放地址
        var webRtcUrl = _zlm.GetWebRtcPlayUrl("rtp", streamId);
        return new PlayResult { Success = true, WebRtcUrl = webRtcUrl };
    }

    public async Task<bool> StopAsync(string deviceId, string channelId)
    {
        var streamId = $"{deviceId}_{channelId}";
        return await _zlm.CloseRtpServerAsync(streamId);
    }
}
```

- [ ] **Step 3: 创建 StreamController**

```csharp
namespace GB28181Platform.Api.Controllers;

[ApiController]
[Route("api/[controller]")]
public class StreamController : ControllerBase
{
    private readonly IStreamAppService _streamService;

    public StreamController(IStreamAppService streamService)
    {
        _streamService = streamService;
    }

    [HttpPost("play")]
    public async Task<IActionResult> Play([FromBody] PlayRequest req)
    {
        var result = await _streamService.PlayAsync(req.DeviceId, req.ChannelId);
        return Ok(result);
    }

    [HttpPost("stop")]
    public async Task<IActionResult> Stop([FromBody] StopRequest req)
    {
        var ok = await _streamService.StopAsync(req.DeviceId, req.ChannelId);
        return Ok(new { success = ok });
    }
}

public class PlayRequest
{
    public string DeviceId { get; set; } = "";
    public string ChannelId { get; set; } = "";
}

public class StopRequest
{
    public string DeviceId { get; set; } = "";
    public string ChannelId { get; set; } = "";
}
```

- [ ] **Step 4: 在 Program.cs 注册服务**

```csharp
builder.Services.AddScoped<IStreamAppService, StreamAppService>();
builder.Services.AddSingleton<InviteHandler>();
```

- [ ] **Step 5: 编译验证**

Run: `dotnet build GB28181Platform.sln`
Expected: Build succeeded.

- [ ] **Step 6: Commit**

```bash
git add src/
git commit -m "feat(stream): add SIP INVITE and ZLMediaKit stream play/stop"
```

---

## Task 6: WebRTC 实时预览前端页面

**Files:**
- Create: `frontend/vms-web/src/api/stream.ts`
- Rewrite: `frontend/vms-web/src/views/LivePreview.vue`

- [ ] **Step 1: 创建 stream API**

```typescript
// frontend/vms-web/src/api/stream.ts
import request from './request'

export function playStream(deviceId: string, channelId: string) {
  return request.post('/stream/play', { deviceId, channelId })
}

export function stopStream(deviceId: string, channelId: string) {
  return request.post('/stream/stop', { deviceId, channelId })
}
```

- [ ] **Step 2: 重写 LivePreview.vue**

实现：
- 左侧设备/通道列表（从 API 获取）
- 右侧 WebRTC 视频播放区域
- 点击通道 → 调用 playStream → 获取 WebRTC URL → 通过 WHEP 建立连接
- 支持多画面（2x2 分屏）
- 科技风样式：深色背景，视频窗口带荧光边框

WebRTC WHEP 连接核心逻辑：

```typescript
async function startWebRTC(webrtcUrl: string, videoEl: HTMLVideoElement) {
  const pc = new RTCPeerConnection()
  pc.addTransceiver('video', { direction: 'recvonly' })
  pc.addTransceiver('audio', { direction: 'recvonly' })

  pc.ontrack = (event) => {
    videoEl.srcObject = event.streams[0]
  }

  const offer = await pc.createOffer()
  await pc.setLocalDescription(offer)

  const resp = await fetch(webrtcUrl, {
    method: 'POST',
    headers: { 'Content-Type': 'application/sdp' },
    body: offer.sdp
  })
  const answerSdp = await resp.text()
  await pc.setRemoteDescription({ type: 'answer', sdp: answerSdp })

  return pc
}
```

- [ ] **Step 3: 验证页面**

Run: `cd frontend/vms-web && npm run dev`
确认页面布局正确（ZLMediaKit 未部署时视频区域显示"等待连接"）。

- [ ] **Step 4: Commit**

```bash
git add frontend/vms-web/src/api/stream.ts frontend/vms-web/src/views/LivePreview.vue
git commit -m "feat(frontend): implement WebRTC live preview page"
```

---

## Task 7: 诊断引擎 — 核心框架

**Files:**
- Create: `src/GB28181Platform.Diagnostic/Steps/IDiagnosticStep.cs`
- Create: `src/GB28181Platform.Diagnostic/Engine/IDiagnosticEngine.cs`
- Create: `src/GB28181Platform.Diagnostic/Engine/DiagnosticEngine.cs`
- Modify: `src/GB28181Platform.Diagnostic/GB28181Platform.Diagnostic.csproj`

- [ ] **Step 1: 添加 NuGet 依赖**

在 `GB28181Platform.Diagnostic.csproj` 中添加对 Domain 项目的引用以及 SqlSugar：

```xml
<ItemGroup>
  <ProjectReference Include="..\GB28181Platform.Domain\GB28181Platform.Domain.csproj" />
  <ProjectReference Include="..\GB28181Platform.Infrastructure\GB28181Platform.Infrastructure.csproj" />
</ItemGroup>
```

- [ ] **Step 2: 创建诊断步骤接口**

```csharp
namespace GB28181Platform.Diagnostic.Steps;

public interface IDiagnosticStep
{
    string StepName { get; }
    DiagnosticStepType StepType { get; }
    Task<StepResult> ExecuteAsync(DiagnosticContext context);
}

public class DiagnosticContext
{
    public string DeviceId { get; set; } = "";
    public string IpAddress { get; set; } = "";
    public int WebPort { get; set; } = 80;
    public string? WebUsername { get; set; }
    public string? WebPassword { get; set; }
    public long TaskId { get; set; }
    public bool ShouldContinue { get; set; } = true;
}

public class StepResult
{
    public bool Success { get; set; }
    public string Detail { get; set; } = "";
    public int DurationMs { get; set; }
    public string? ScreenshotPath { get; set; }
    public bool ContinueNext { get; set; } = true;
}
```

- [ ] **Step 3: 创建诊断引擎**

```csharp
namespace GB28181Platform.Diagnostic.Engine;

public interface IDiagnosticEngine
{
    Task RunDiagnosticAsync(long taskId, string deviceId);
}

public class DiagnosticEngine : IDiagnosticEngine
{
    private readonly IEnumerable<IDiagnosticStep> _steps;
    private readonly ISqlSugarClient _db;
    private readonly ILogger<DiagnosticEngine> _logger;

    public DiagnosticEngine(
        IEnumerable<IDiagnosticStep> steps,
        ISqlSugarClient db,
        ILogger<DiagnosticEngine> logger)
    {
        _steps = steps.OrderBy(s => s.StepType);
        _db = db;
        _logger = logger;
    }

    public async Task RunDiagnosticAsync(long taskId, string deviceId)
    {
        // 查询设备信息
        var device = await _db.Queryable<Device>().FirstAsync(d => d.Id == deviceId);
        if (device == null)
        {
            _logger.LogWarning("诊断失败: 设备 {DeviceId} 不存在", deviceId);
            return;
        }

        // 更新任务状态
        await _db.Updateable<DiagnosticTask>()
            .SetColumns(t => t.Status == 1) // Running
            .SetColumns(t => t.StartedAt == DateTime.Now)
            .Where(t => t.Id == taskId)
            .ExecuteCommandAsync();

        var context = new DiagnosticContext
        {
            DeviceId = deviceId,
            IpAddress = device.RemoteIp ?? "",
            WebPort = device.WebPort > 0 ? device.WebPort : 80,
            WebUsername = device.WebUsername,
            WebPassword = device.WebPassword,
            TaskId = taskId
        };

        var conclusions = new List<string>();

        foreach (var step in _steps)
        {
            if (!context.ShouldContinue) break;

            _logger.LogInformation("诊断 {DeviceId} - 执行步骤: {Step}", deviceId, step.StepName);
            var result = await step.ExecuteAsync(context);

            // 保存日志
            var log = new DiagnosticLog
            {
                TaskId = taskId,
                DeviceId = deviceId,
                StepName = step.StepName,
                StepType = (int)step.StepType,
                Success = result.Success,
                Detail = result.Detail,
                DurationMs = result.DurationMs,
                ScreenshotPath = result.ScreenshotPath,
                CreatedAt = DateTime.Now
            };
            await _db.Insertable(log).ExecuteCommandAsync();

            conclusions.Add($"[{step.StepName}] {(result.Success ? "通过" : "失败")}: {result.Detail}");
            context.ShouldContinue = result.ContinueNext;
        }

        // 更新任务结论
        var conclusion = string.Join("\n", conclusions);
        await _db.Updateable<DiagnosticTask>()
            .SetColumns(t => t.Status == 2) // Completed
            .SetColumns(t => t.CompletedAt == DateTime.Now)
            .SetColumns(t => t.Conclusion == conclusion)
            .Where(t => t.Id == taskId)
            .ExecuteCommandAsync();

        _logger.LogInformation("诊断完成 {DeviceId}: {Conclusion}", deviceId, conclusion);
    }
}
```

- [ ] **Step 4: 编译验证**

Run: `dotnet build GB28181Platform.sln`

- [ ] **Step 5: Commit**

```bash
git add src/GB28181Platform.Diagnostic/
git commit -m "feat(diagnostic): add diagnostic engine framework with step pipeline"
```

---

## Task 8: 诊断步骤 — Ping + Port

**Files:**
- Create: `src/GB28181Platform.Diagnostic/Steps/PingCheckStep.cs`
- Create: `src/GB28181Platform.Diagnostic/Steps/PortCheckStep.cs`

- [ ] **Step 1: 实现 PingCheckStep**

```csharp
namespace GB28181Platform.Diagnostic.Steps;

public class PingCheckStep : IDiagnosticStep
{
    public string StepName => "ICMP Ping";
    public DiagnosticStepType StepType => DiagnosticStepType.Ping;

    public async Task<StepResult> ExecuteAsync(DiagnosticContext context)
    {
        var sw = System.Diagnostics.Stopwatch.StartNew();
        try
        {
            using var ping = new System.Net.NetworkInformation.Ping();
            var reply = await ping.SendPingAsync(context.IpAddress, 5000);
            sw.Stop();

            if (reply.Status == System.Net.NetworkInformation.IPStatus.Success)
            {
                return new StepResult
                {
                    Success = true,
                    Detail = $"Ping 可达, RTT={reply.RoundtripTime}ms",
                    DurationMs = (int)sw.ElapsedMilliseconds,
                    ContinueNext = true
                };
            }

            return new StepResult
            {
                Success = false,
                Detail = $"Ping 超时, 状态={reply.Status}",
                DurationMs = (int)sw.ElapsedMilliseconds,
                ContinueNext = false // 网络不通，不继续
            };
        }
        catch (Exception ex)
        {
            sw.Stop();
            return new StepResult
            {
                Success = false,
                Detail = $"Ping 异常: {ex.Message}",
                DurationMs = (int)sw.ElapsedMilliseconds,
                ContinueNext = false
            };
        }
    }
}
```

- [ ] **Step 2: 实现 PortCheckStep**

```csharp
namespace GB28181Platform.Diagnostic.Steps;

public class PortCheckStep : IDiagnosticStep
{
    public string StepName => "TCP 端口检测";
    public DiagnosticStepType StepType => DiagnosticStepType.PortCheck;

    public async Task<StepResult> ExecuteAsync(DiagnosticContext context)
    {
        var sw = System.Diagnostics.Stopwatch.StartNew();
        var port = context.WebPort;

        try
        {
            using var client = new System.Net.Sockets.TcpClient();
            var connectTask = client.ConnectAsync(context.IpAddress, port);
            var completed = await Task.WhenAny(connectTask, Task.Delay(5000));
            sw.Stop();

            if (completed == connectTask && client.Connected)
            {
                return new StepResult
                {
                    Success = true,
                    Detail = $"端口 {port} 可达",
                    DurationMs = (int)sw.ElapsedMilliseconds,
                    ContinueNext = true
                };
            }

            return new StepResult
            {
                Success = false,
                Detail = $"端口 {port} 不可达 (超时 5s)",
                DurationMs = (int)sw.ElapsedMilliseconds,
                ContinueNext = false
            };
        }
        catch (Exception ex)
        {
            sw.Stop();
            return new StepResult
            {
                Success = false,
                Detail = $"端口 {port} 连接失败: {ex.Message}",
                DurationMs = (int)sw.ElapsedMilliseconds,
                ContinueNext = false
            };
        }
    }
}
```

- [ ] **Step 3: 编译验证**

Run: `dotnet build GB28181Platform.sln`

- [ ] **Step 4: Commit**

```bash
git add src/GB28181Platform.Diagnostic/Steps/
git commit -m "feat(diagnostic): implement Ping and TCP port check steps"
```

---

## Task 9: 诊断步骤 — 浏览器检查 (Playwright + Qwen 视觉)

**Files:**
- Create: `src/GB28181Platform.Diagnostic/Steps/BrowserCheckStep.cs`
- Create: `src/GB28181Platform.Diagnostic/Browser/CameraBrowserAgent.cs`
- Modify: `src/GB28181Platform.Diagnostic/GB28181Platform.Diagnostic.csproj`

- [ ] **Step 1: 添加 Playwright NuGet 包**

在 `GB28181Platform.Diagnostic.csproj` 添加：

```xml
<PackageReference Include="Microsoft.Playwright" Version="1.49.0" />
```

- [ ] **Step 2: 创建 CameraBrowserAgent**

```csharp
namespace GB28181Platform.Diagnostic.Browser;

using Microsoft.Playwright;

public class CameraBrowserAgent
{
    private readonly HttpClient _qwenHttp;
    private readonly IConfiguration _config;
    private readonly ILogger<CameraBrowserAgent> _logger;

    public CameraBrowserAgent(IConfiguration config, ILogger<CameraBrowserAgent> logger)
    {
        _config = config;
        _logger = logger;
        _qwenHttp = new HttpClient
        {
            BaseAddress = new Uri(config["QwenApi:BaseUrl"] ?? "http://localhost:8000")
        };
    }

    public async Task<BrowserCheckResult> CheckCameraConfigAsync(
        string ip, int port, string? username, string? password, string expectedSipServerIp, string expectedServerId)
    {
        var screenshotDir = Path.Combine("screenshots", DateTime.Now.ToString("yyyyMMdd"));
        Directory.CreateDirectory(screenshotDir);
        var screenshotPath = Path.Combine(screenshotDir, $"{ip}_{DateTime.Now:HHmmss}.png");

        try
        {
            using var playwright = await Playwright.CreateAsync();
            await using var browser = await playwright.Chromium.LaunchAsync(new() { Headless = true });
            var page = await browser.NewPageAsync();

            // 访问摄像机网页
            var url = $"http://{ip}:{port}";
            await page.GotoAsync(url, new() { Timeout = 15000 });
            await page.WaitForLoadStateAsync(LoadState.NetworkIdle);

            // 截图
            await page.ScreenshotAsync(new() { Path = screenshotPath, FullPage = true });
            _logger.LogInformation("摄像机网页截图已保存: {Path}", screenshotPath);

            // 调用 Qwen 视觉模型分析截图
            var analysis = await AnalyzeScreenshotAsync(screenshotPath, expectedSipServerIp, expectedServerId);

            return new BrowserCheckResult
            {
                Success = true,
                ScreenshotPath = screenshotPath,
                Analysis = analysis
            };
        }
        catch (Exception ex)
        {
            _logger.LogError(ex, "浏览器检查失败: {Ip}:{Port}", ip, port);
            return new BrowserCheckResult
            {
                Success = false,
                ScreenshotPath = screenshotPath,
                Analysis = $"浏览器访问失败: {ex.Message}"
            };
        }
    }

    private async Task<string> AnalyzeScreenshotAsync(string screenshotPath, string expectedSipIp, string expectedServerId)
    {
        var imageBytes = await File.ReadAllBytesAsync(screenshotPath);
        var base64 = Convert.ToBase64String(imageBytes);

        var prompt = $@"这是一台摄像机的网页管理界面截图。请分析截图中是否能看到 GB28181/国标 相关配置。
如果能看到，请提取以下信息：
- SIP 服务器 IP
- SIP 服务器端口
- 设备编码
- SIP 域/realm

然后与期望配置对比：
- 期望 SIP 服务器 IP: {expectedSipIp}
- 期望服务器编码: {expectedServerId}

如果有差异请指出，如果看不到国标配置页面请说明。";

        var requestBody = new
        {
            model = _config["QwenApi:Model"] ?? "qwen-vl-plus",
            messages = new[]
            {
                new
                {
                    role = "user",
                    content = new object[]
                    {
                        new { type = "image_url", image_url = new { url = $"data:image/png;base64,{base64}" } },
                        new { type = "text", text = prompt }
                    }
                }
            },
            max_tokens = 1000
        };

        var apiKey = _config["QwenApi:ApiKey"] ?? "";
        var request = new HttpRequestMessage(HttpMethod.Post, "/v1/chat/completions");
        request.Headers.Add("Authorization", $"Bearer {apiKey}");
        request.Content = JsonContent.Create(requestBody);

        var response = await _qwenHttp.SendAsync(request);
        var json = await response.Content.ReadFromJsonAsync<JsonElement>();

        var content = json.GetProperty("choices")[0]
            .GetProperty("message")
            .GetProperty("content")
            .GetString();

        return content ?? "分析结果为空";
    }
}

public class BrowserCheckResult
{
    public bool Success { get; set; }
    public string ScreenshotPath { get; set; } = "";
    public string Analysis { get; set; } = "";
}
```

- [ ] **Step 3: 创建 BrowserCheckStep**

```csharp
namespace GB28181Platform.Diagnostic.Steps;

public class BrowserCheckStep : IDiagnosticStep
{
    private readonly CameraBrowserAgent _agent;
    private readonly IConfiguration _config;

    public BrowserCheckStep(CameraBrowserAgent agent, IConfiguration config)
    {
        _agent = agent;
        _config = config;
    }

    public string StepName => "浏览器配置检查";
    public DiagnosticStepType StepType => DiagnosticStepType.BrowserCheck;

    public async Task<StepResult> ExecuteAsync(DiagnosticContext context)
    {
        var sw = System.Diagnostics.Stopwatch.StartNew();

        var expectedSipIp = _config["SipServer:ListenIp"] ?? "0.0.0.0";
        var expectedServerId = _config["SipServer:ServerId"] ?? "";

        var result = await _agent.CheckCameraConfigAsync(
            context.IpAddress, context.WebPort,
            context.WebUsername, context.WebPassword,
            expectedSipIp, expectedServerId);

        sw.Stop();

        return new StepResult
        {
            Success = result.Success,
            Detail = result.Analysis,
            DurationMs = (int)sw.ElapsedMilliseconds,
            ScreenshotPath = result.ScreenshotPath,
            ContinueNext = false // 最后一步
        };
    }
}
```

- [ ] **Step 4: 编译验证**

Run: `dotnet build GB28181Platform.sln`

- [ ] **Step 5: Commit**

```bash
git add src/GB28181Platform.Diagnostic/
git commit -m "feat(diagnostic): implement browser check step with Playwright + Qwen vision"
```

---

## Task 10: 诊断引擎接入 API + 自动触发

**Files:**
- Modify: `src/GB28181Platform.Api/Controllers/DiagnosticController.cs`
- Create: `src/GB28181Platform.Api/BackgroundServices/DiagnosticWorkerService.cs`
- Modify: `src/GB28181Platform.Api/BackgroundServices/DeviceMonitorService.cs`
- Modify: `src/GB28181Platform.Api/Program.cs`

- [ ] **Step 1: 创建 DiagnosticWorkerService**

后台队列处理诊断任务：

```csharp
namespace GB28181Platform.Api.BackgroundServices;

public class DiagnosticWorkerService : BackgroundService
{
    private readonly Channel<DiagnosticRequest> _queue;
    private readonly IServiceScopeFactory _scopeFactory;
    private readonly ILogger<DiagnosticWorkerService> _logger;

    public DiagnosticWorkerService(
        Channel<DiagnosticRequest> queue,
        IServiceScopeFactory scopeFactory,
        ILogger<DiagnosticWorkerService> logger)
    {
        _queue = queue;
        _scopeFactory = scopeFactory;
        _logger = logger;
    }

    protected override async Task ExecuteAsync(CancellationToken stoppingToken)
    {
        _logger.LogInformation("诊断工作线程已启动");
        await foreach (var request in _queue.Reader.ReadAllAsync(stoppingToken))
        {
            try
            {
                using var scope = _scopeFactory.CreateScope();
                var engine = scope.ServiceProvider.GetRequiredService<IDiagnosticEngine>();
                await engine.RunDiagnosticAsync(request.TaskId, request.DeviceId);
            }
            catch (Exception ex)
            {
                _logger.LogError(ex, "诊断任务执行失败: TaskId={TaskId}", request.TaskId);
            }
        }
    }
}

public class DiagnosticRequest
{
    public long TaskId { get; set; }
    public string DeviceId { get; set; } = "";
}
```

- [ ] **Step 2: 修改 DiagnosticController 接入队列**

在 POST /run/{deviceId} 中，创建任务后将其放入 Channel 队列。

- [ ] **Step 3: 修改 DeviceMonitorService 自动触发诊断**

当设备离线时，自动创建诊断任务并放入队列。

- [ ] **Step 4: 在 Program.cs 注册所有诊断服务**

```csharp
// 诊断引擎
builder.Services.AddScoped<IDiagnosticEngine, DiagnosticEngine>();
builder.Services.AddScoped<IDiagnosticStep, PingCheckStep>();
builder.Services.AddScoped<IDiagnosticStep, PortCheckStep>();
builder.Services.AddScoped<IDiagnosticStep, BrowserCheckStep>();
builder.Services.AddSingleton<CameraBrowserAgent>();

// 诊断任务队列
builder.Services.AddSingleton(Channel.CreateUnbounded<DiagnosticRequest>());
builder.Services.AddHostedService<DiagnosticWorkerService>();
```

- [ ] **Step 5: 编译验证**

Run: `dotnet build GB28181Platform.sln`

- [ ] **Step 6: Commit**

```bash
git add src/
git commit -m "feat(diagnostic): wire up diagnostic engine with API and auto-trigger on offline"
```

---

## Task 11: AI Agent — Qwen 客户端 + Function Calling

**Files:**
- Create: `src/GB28181Platform.AiAgent/IQwenClient.cs`
- Create: `src/GB28181Platform.AiAgent/QwenClient.cs`
- Create: `src/GB28181Platform.AiAgent/Functions/IAgentFunction.cs`
- Create: `src/GB28181Platform.AiAgent/Functions/FunctionRegistry.cs`
- Create: `src/GB28181Platform.AiAgent/Functions/GetDeviceStatusFunction.cs`
- Create: `src/GB28181Platform.AiAgent/Functions/GetDiagnosticLogsFunction.cs`
- Create: `src/GB28181Platform.AiAgent/Functions/ListOfflineDevicesFunction.cs`
- Modify: `src/GB28181Platform.AiAgent/GB28181Platform.AiAgent.csproj`

- [ ] **Step 1: 添加依赖**

```xml
<ItemGroup>
  <ProjectReference Include="..\GB28181Platform.Domain\GB28181Platform.Domain.csproj" />
  <ProjectReference Include="..\GB28181Platform.Infrastructure\GB28181Platform.Infrastructure.csproj" />
</ItemGroup>
```

- [ ] **Step 2: 创建 QwenClient**

实现 OpenAI 兼容的 chat/completions 调用，支持 function_call 响应解析。

```csharp
namespace GB28181Platform.AiAgent;

public interface IQwenClient
{
    Task<ChatResponse> ChatAsync(List<ChatMessage> messages, List<FunctionDefinition>? functions = null);
}

public class ChatMessage
{
    public string Role { get; set; } = "";
    public string? Content { get; set; }
    public string? Name { get; set; }
    public FunctionCall? FunctionCall { get; set; }
}

public class FunctionCall
{
    public string Name { get; set; } = "";
    public string Arguments { get; set; } = "";
}

public class FunctionDefinition
{
    public string Name { get; set; } = "";
    public string Description { get; set; } = "";
    public object Parameters { get; set; } = new { };
}

public class ChatResponse
{
    public string? Content { get; set; }
    public FunctionCall? FunctionCall { get; set; }
    public string FinishReason { get; set; } = "";
}
```

```csharp
namespace GB28181Platform.AiAgent;

public class QwenClient : IQwenClient
{
    private readonly HttpClient _http;
    private readonly string _model;

    public QwenClient(IConfiguration config)
    {
        var baseUrl = config["QwenApi:BaseUrl"] ?? "http://localhost:8000";
        var apiKey = config["QwenApi:ApiKey"] ?? "";
        _model = config["QwenApi:Model"] ?? "qwen-3.5";

        _http = new HttpClient { BaseAddress = new Uri(baseUrl) };
        _http.DefaultRequestHeaders.Add("Authorization", $"Bearer {apiKey}");
    }

    public async Task<ChatResponse> ChatAsync(List<ChatMessage> messages, List<FunctionDefinition>? functions = null)
    {
        var body = new Dictionary<string, object>
        {
            ["model"] = _model,
            ["messages"] = messages.Select(m => new
            {
                role = m.Role,
                content = m.Content,
                name = m.Name,
                function_call = m.FunctionCall == null ? null : new { name = m.FunctionCall.Name, arguments = m.FunctionCall.Arguments }
            }).ToList()
        };

        if (functions != null && functions.Count > 0)
        {
            body["functions"] = functions;
            body["function_call"] = "auto";
        }

        var response = await _http.PostAsJsonAsync("/v1/chat/completions", body);
        response.EnsureSuccessStatusCode();
        var json = await response.Content.ReadFromJsonAsync<JsonElement>();

        var choice = json.GetProperty("choices")[0];
        var message = choice.GetProperty("message");
        var finishReason = choice.GetProperty("finish_reason").GetString() ?? "";

        var result = new ChatResponse { FinishReason = finishReason };

        if (message.TryGetProperty("function_call", out var fc))
        {
            result.FunctionCall = new FunctionCall
            {
                Name = fc.GetProperty("name").GetString() ?? "",
                Arguments = fc.GetProperty("arguments").GetString() ?? "{}"
            };
        }
        else
        {
            result.Content = message.GetProperty("content").GetString();
        }

        return result;
    }
}
```

- [ ] **Step 3: 创建 Function 接口和注册表**

```csharp
namespace GB28181Platform.AiAgent.Functions;

public interface IAgentFunction
{
    string Name { get; }
    string Description { get; }
    object ParameterSchema { get; }
    Task<string> ExecuteAsync(string arguments);
}

public class FunctionRegistry
{
    private readonly Dictionary<string, IAgentFunction> _functions = new();

    public FunctionRegistry(IEnumerable<IAgentFunction> functions)
    {
        foreach (var f in functions)
            _functions[f.Name] = f;
    }

    public IAgentFunction? Get(string name) => _functions.GetValueOrDefault(name);
    public List<FunctionDefinition> GetDefinitions() => _functions.Values.Select(f => new FunctionDefinition
    {
        Name = f.Name,
        Description = f.Description,
        Parameters = f.ParameterSchema
    }).ToList();
}
```

- [ ] **Step 4: 实现具体 Function**

GetDeviceStatusFunction: 查询设备状态和最后心跳时间
GetDiagnosticLogsFunction: 查询最近诊断日志
ListOfflineDevicesFunction: 列出所有离线设备

每个 Function 内部使用 SqlSugar 查询数据库，将结果序列化为 JSON 字符串返回。

- [ ] **Step 5: 编译验证**

Run: `dotnet build GB28181Platform.sln`

- [ ] **Step 6: Commit**

```bash
git add src/GB28181Platform.AiAgent/
git commit -m "feat(ai): implement Qwen client with function calling support"
```

---

## Task 12: AI Agent 服务 + 接入 API

**Files:**
- Create: `src/GB28181Platform.AiAgent/IAiAgentService.cs`
- Create: `src/GB28181Platform.AiAgent/AiAgentService.cs`
- Create: `src/GB28181Platform.AiAgent/Prompts/SystemPrompts.cs`
- Modify: `src/GB28181Platform.Api/Controllers/AiAgentController.cs`
- Modify: `src/GB28181Platform.Api/Program.cs`

- [ ] **Step 1: 创建 System Prompt**

```csharp
namespace GB28181Platform.AiAgent.Prompts;

public static class SystemPrompts
{
    public const string DiagnosticAssistant = @"你是 GB28181 视频监控平台的智能运维助手。
你的职责是帮助运维人员诊断和分析摄像机设备的问题。

你可以：
1. 查询设备状态和在线情况
2. 查看诊断日志和历史记录
3. 分析设备离线原因并给出修复建议

回答要求：
- 用中文回答
- 先说结论，再给详细分析
- 如果发现配置错误，给出具体的修改建议
- 引用诊断日志中的具体数据（如 RTT、端口状态等）";
}
```

- [ ] **Step 2: 创建 AiAgentService (Function Calling 循环)**

```csharp
namespace GB28181Platform.AiAgent;

public interface IAiAgentService
{
    Task<string> ChatAsync(string sessionId, string userMessage, string? deviceId = null);
}

public class AiAgentService : IAiAgentService
{
    private readonly IQwenClient _qwen;
    private readonly FunctionRegistry _registry;
    private readonly ISqlSugarClient _db;
    private readonly ILogger<AiAgentService> _logger;
    private const int MaxFunctionCalls = 5;

    public AiAgentService(IQwenClient qwen, FunctionRegistry registry,
        ISqlSugarClient db, ILogger<AiAgentService> logger)
    {
        _qwen = qwen;
        _registry = registry;
        _db = db;
        _logger = logger;
    }

    public async Task<string> ChatAsync(string sessionId, string userMessage, string? deviceId = null)
    {
        var messages = new List<ChatMessage>
        {
            new() { Role = "system", Content = SystemPrompts.DiagnosticAssistant },
            new() { Role = "user", Content = userMessage }
        };

        var functions = _registry.GetDefinitions();
        var callCount = 0;

        while (callCount < MaxFunctionCalls)
        {
            var response = await _qwen.ChatAsync(messages, functions);

            if (response.FunctionCall != null)
            {
                callCount++;
                _logger.LogInformation("AI 调用函数: {Name}({Args})",
                    response.FunctionCall.Name, response.FunctionCall.Arguments);

                var function = _registry.Get(response.FunctionCall.Name);
                var result = function != null
                    ? await function.ExecuteAsync(response.FunctionCall.Arguments)
                    : $"未知函数: {response.FunctionCall.Name}";

                messages.Add(new() { Role = "assistant", FunctionCall = response.FunctionCall });
                messages.Add(new() { Role = "function", Name = response.FunctionCall.Name, Content = result });
            }
            else
            {
                // 保存对话到数据库
                await SaveConversationAsync(sessionId, deviceId, userMessage, response.Content ?? "");
                return response.Content ?? "抱歉，我无法回答这个问题。";
            }
        }

        return "抱歉，分析过程超出了最大调用次数限制。";
    }

    private async Task SaveConversationAsync(string sessionId, string? deviceId, string userMsg, string assistantMsg)
    {
        var records = new List<AiConversation>
        {
            new() { SessionId = sessionId, DeviceId = deviceId, Role = "user", Content = userMsg, CreatedAt = DateTime.Now },
            new() { SessionId = sessionId, DeviceId = deviceId, Role = "assistant", Content = assistantMsg, CreatedAt = DateTime.Now }
        };
        await _db.Insertable(records).ExecuteCommandAsync();
    }
}
```

- [ ] **Step 3: 修改 AiAgentController**

替换占位实现，接入 AiAgentService。

```csharp
[HttpPost("chat")]
public async Task<IActionResult> Chat([FromBody] ChatRequest req)
{
    var reply = await _aiAgent.ChatAsync(req.SessionId, req.Message, req.DeviceId);
    return Ok(ApiResponse<object>.Ok(new { reply }));
}
```

- [ ] **Step 4: 在 Program.cs 注册 AI 服务**

```csharp
builder.Services.AddSingleton<IQwenClient, QwenClient>();
builder.Services.AddScoped<IAgentFunction, GetDeviceStatusFunction>();
builder.Services.AddScoped<IAgentFunction, GetDiagnosticLogsFunction>();
builder.Services.AddScoped<IAgentFunction, ListOfflineDevicesFunction>();
builder.Services.AddScoped<FunctionRegistry>();
builder.Services.AddScoped<IAiAgentService, AiAgentService>();
```

- [ ] **Step 5: 编译验证**

Run: `dotnet build GB28181Platform.sln`

- [ ] **Step 6: Commit**

```bash
git add src/
git commit -m "feat(ai): implement AI agent service with function calling loop"
```

---

## Task 13: 科技风改造诊断面板 + AI 问答页

**Files:**
- Modify: `frontend/vms-web/src/views/DiagnosticPanel.vue`
- Modify: `frontend/vms-web/src/views/AiChat.vue`

- [ ] **Step 1: 改造 DiagnosticPanel.vue**

- 时间线样式展示诊断步骤（Ping → Port → Browser）
- 每步展示成功/失败状态图标 + 耗时
- 截图预览（Browser 步骤）
- 手动诊断按钮带荧光边框
- 诊断结论区域使用 tech-mono 字体

- [ ] **Step 2: 改造 AiChat.vue**

- 类终端/Chat 风格，深色背景
- 用户消息靠右，荧光蓝边框
- AI 回复靠左，暗色卡片 + 打字机效果
- 输入框底部固定，带发送按钮
- 快捷问题按钮："离线设备列表"、"为什么XX离线了"

打字机效果实现：
```typescript
function typewriterEffect(text: string, callback: (partial: string) => void) {
  let i = 0
  const interval = setInterval(() => {
    callback(text.slice(0, ++i))
    if (i >= text.length) clearInterval(interval)
  }, 20)
}
```

- [ ] **Step 3: 验证效果**

Run: `cd frontend/vms-web && npm run dev`

- [ ] **Step 4: Commit**

```bash
git add frontend/vms-web/src/views/
git commit -m "feat(frontend): redesign diagnostic panel and AI chat with sci-fi theme"
```

---

## Task 14: 集成测试 + 最终验证

**Files:**
- Modify: `src/GB28181Platform.Api/Program.cs` (最终 DI 注册检查)
- 全项目编译和运行测试

- [ ] **Step 1: 全项目编译**

Run: `dotnet build GB28181Platform.sln`
Expected: Build succeeded. 0 Errors. 0 Warnings.

- [ ] **Step 2: 启动后端验证**

Run: `cd src/GB28181Platform.Api && dotnet run`
验证：
- 控制台输出 SIP Server started on :5060
- 访问 http://localhost:5000 返回健康检查
- 访问 http://localhost:5000/api/device 返回空列表

- [ ] **Step 3: 启动前端验证**

Run: `cd frontend/vms-web && npm run dev`
验证：
- 科技风暗色主题正确
- 4 个页面均可访问
- 导航切换正常

- [ ] **Step 4: 最终 Commit + Push**

```bash
git add -A
git commit -m "chore: final integration verification"
git push origin main
```

---

## 自审检查

### Spec 覆盖

| Spec 需求 | 对应 Task |
|-----------|----------|
| F1: GB28181 设备注册 | Phase 1 已完成 (SIP 注册/心跳/认证) |
| F2: Web 视频预览 | Task 4 (ZLM Client) + Task 5 (INVITE+拉流) + Task 6 (WebRTC 前端) |
| F3: 设备状态监控 | Phase 1 已完成 (DeviceMonitorService + SignalR) |
| F4: 离线自动诊断 | Task 7 (引擎框架) + Task 8 (Ping/Port) + Task 9 (Browser) + Task 10 (接入) |
| F5: AI 问答 Agent | Task 11 (Qwen Client) + Task 12 (Agent Service) |
| .NET 8 降级 | Task 1 |
| 科技风 UI | Task 2 (主题) + Task 3 (设备列表) + Task 13 (诊断+AI) |
| 非功能: 100-500路 | 架构支撑，ZLMediaKit 单机数千路 |

### 类型一致性检查

- `IDiagnosticStep` 在 Task 7 定义，Task 8/9 实现 — 签名一致 ✅
- `IDiagnosticEngine` 在 Task 7 定义，Task 10 注入 — 一致 ✅
- `IQwenClient` 在 Task 11 定义，Task 12 使用 — 一致 ✅
- `IAgentFunction` 在 Task 11 定义，Task 12 注册 — 一致 ✅
- `IZlmClient` 在 Task 4 定义，Task 5 使用 — 一致 ✅
- `PlayResult` 在 Task 5 定义并返回 — 一致 ✅
